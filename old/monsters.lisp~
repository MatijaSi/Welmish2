(defpackage monsters
  (:use :cl))

(in-package monsters)

(defclass monster (movable:mover combat:combatant) ())

(defmethod control ((monster monster) level)
  (movable:move (- (random 3) 1) (- (random 3) 1)) level)
